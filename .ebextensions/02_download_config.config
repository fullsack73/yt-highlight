# .ebextensions/02_download_config.config

commands:
  01_download_cookie_file:
    # 애플리케이션 배포 전에 S3에서 쿠키 파일을 /tmp 디렉토리로 복사합니다.
    command: "aws s3 cp s3://yt-hl-app-config-0001/cookies.txt /tmp/cookies.txt"
    # 이 명령이 실패하면 배포가 중단되도록 합니다.
    ignoreErrors: false